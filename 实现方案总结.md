# Agent-扩散策略协同的类人双臂果蔬分拣机器人 实现方案

本文档是对原始架构图与后续全部讨论的收敛结论。凡是讨论中被修改、砍掉或明确否决的部分，都在第 10 节单独列出，便于和最初的设计图对照。

## 1. 课题与总体目标

构建一套「慢循环 Agent 决策 + 快循环扩散策略执行」的类人双臂果蔬分拣机器人系统。传送带上游持续送料、果蔬在料箱/传送带上杂乱甚至堆叠、部分目标被遮挡且处于运动中。

系统的核心分工只有一条：**慢循环负责"抓哪个"，快循环负责"怎么抓"**。

预期指标（讨论中确定的验收口径）：

| 指标 | 目标 |
| --- | --- |
| 训练过的 8 类果蔬整体抓取成功率 | 85-95% |
| 草莓、荔枝等小果 | 80-90% |
| 慢循环技能路由准确率 | >95% |
| 快循环单周期 | 20Hz（50ms 内完成感知→推理→下发） |
| 训练成本 | 单张 RTX 4090，4-6 小时 |

## 2. 系统总架构

```
[RGB-D 相机 + (SWIR) + 传送带编码器 + 触觉传感器]
                    ↓  多传感器时空对齐 / 手眼标定
[结构化环境状态: id, class, mask, bbox, pose, velocity, tactile]
                    ↓
[慢循环 Agent / LangGraph]  经验记忆检索 → VLM 推理 → 结构化 Manipulation Goal
                    ↓  /manipulation_goal (ROS2 Topic)
[快循环 Skill-Routed Diffusion Policy]  技能路由器 → 5 个专家 → 共享扩散解码器
                    ↓  Action Chunk (H 步)
[双臂机器人执行 / ROS2 ros2_control]
                    ↓  触觉 + 执行状态
[触觉闭环与异常处理] → 回写经验记忆 / 触发重规划
```

与原图的对应关系：四层（多模态感知 / 结构化状态 / 经验增强 Agent / 技能路由扩散策略）+ 双循环 + 经验记忆全部保留，改动集中在**专家分类**、**输入字段**和**泛化策略**上（见第 5、6、10 节）。

## 3. 感知层

### 3.1 传感器与分工

| 传感器 | 作用 |
| --- | --- |
| RGB 相机 | 目标检测、实例分割、外观/成熟度 |
| 深度相机 | 目标 3D 位置反投影、局部几何 |
| SWIR 相机（可选，后续） | 缺陷/成熟度分析，需自建数据集与轻量 CNN |
| 传送带编码器 | 物体速度（主速度源） |
| 触觉传感器 | 接触力、滑移、接触稳定性（快循环闭环） |

多传感器必须做硬件级触发同步（PTP）与严格时间戳对齐；20ms 的对齐误差在 0.5m/s 传送带上就是 1cm 的抓取偏差。

### 3.2 检测与实例分割：YOLOv8-seg

选型理由：30-60 FPS、精度足够、训练成本低。果蔬的 Mask 不需要像素级完美，FastSAM 精度不够、SAM 2 太慢、Mask R-CNN 太慢。

关键配置：`conf=0.4`（宁可低置信度也别漏检遮挡目标）、`retina_masks=True`（输出原图分辨率 Mask，便于深度反投影）；TensorRT 加速后 8-15ms。

训练数据：Isaac Sim 中按 object path 规则批量加语义标签，自动导出 YOLO 多边形标注，零人工成本；再用 Label Studio / CVAT 标 50-100 张真实图做微调。

### 3.3 目标追踪：ByteTrack

选它是因为它利用低置信度检测框，果蔬被机械臂短暂遮挡时不掉 ID。参数：`track_thresh=0.4`、`track_buffer=30-60`、`match_thresh=0.8`、`frame_rate=30`。

Mask 与 track 用 IoU（阈值 0.5）匹配。输出统一的 `{track_id, class, mask, bbox, pose, velocity}`。

### 3.4 位姿估计：Depth + Mask 反投影（已定）

果蔬近似球体、朝向不重要，只需要位置 (x, y, z)，所以不用 FoundationPose / AnyGrasp：

| 方案 | 速度 | 精度 | 结论 |
| --- | --- | --- | --- |
| Depth + Mask 反投影 | <5ms | ±3-5mm（0.3-0.8m） | **采用** |
| FoundationPose | 30-100ms | 极高 | 需要 CAD、对遮挡敏感，不用 |
| AnyGrasp | 50-200ms | 高 | 需要点云，堆叠下敏感，不用 |

要点：Mask 先腐蚀去掉边缘像素；用 Mask 区域内有效深度的**中值**（不是均值）抗离群点；深度图先去噪补洞；像素 + 深度 + 内参 → 相机系 3D 点 → 手眼标定矩阵 → 机器人基座系。

手眼标定：用 ArUco / 棋盘格在 15-20 个位姿下采集，验证误差 <5mm，推荐 Kalibr 或 EasyHandEye；标定误差是抓取失败的常见根因。

### 3.5 速度估计：传送带编码器为主（已定）

果蔬在传送带上稳定跟随、不翻滚，所以物体速度 ≈ 传送带速度，且角速度 ≈ 0，不需要复杂的视觉速度估计。

硬件链路：增量式光电编码器（1000 PPR，A/B/Z 差分，如欧姆龙 E6B2-CWZ6C）→ 光耦隔离 → STM32 定时器正交解码（硬件计数不丢脉冲，50Hz 计算）→ 串口 115200 → 工控机 ROS2 节点。

工程要点：编码器装在**从动轮或惰轮**上（避免电机启停抖动）；轮径实测标定；采样 50Hz、ROS2 端 50Hz 发布。

多源融合（推荐）：编码器先验（<1ms）+ 多帧位姿差分（10ms，5 帧滑窗最小二乘，用于检测打滑/碰撞/被抬起）→ 卡尔曼/中值滤波 → `/target/velocity` (30Hz)。用高度判断物体是否仍在传送带上，已被抬起就停用传送带先验。

抓取时机预测：`future_pose = pose + velocity × robot_delay`，`robot_delay`（机械臂到位规划时间，约 0.1-0.3s）需实测标定。

## 4. 慢循环 Agent（决策与记忆）

### 4.1 职责与实现

LangGraph 状态机 + 经验记忆 + VLM/LLM 推理；输出严格结构化的 Manipulation Goal。必须启用 checkpointer（生产用 SQLite/持久化）并传 `thread_id`，以便 `interrupt()` 人工审批后恢复。

节点链路：感知状态 → 经验检索 → Agent 推理（场景理解 / 任务规划 / 目标生成）→ 输出 Goal → 下发 → 结果评估 → 写回经验记忆。

结构化输出用 Outlines 在生成阶段约束 JSON Schema，保证 100% 合法，避免下游解析崩溃。

### 4.2 经验记忆

存储三类案例：成功任务、失败任务、更差决策案例。每条记录结构为「任务上下文 + 决策 + 结果」，检索时用当前状态相似度取 Top-K。

失败案例比成功案例更有价值，是自我纠错的来源。讨论中明确**不做** RLHF/DPO，记忆只做检索增强，不用于微调决策模型。

### 4.3 目标选择：硬过滤 + 软评分

先硬性排除不可行目标（可见比例过低、抓取点被遮挡、已被其他臂占用），再按五维加权评分排序：

1. 可见性（visible_ratio）
2. 空间位置 / 顶部优先（height，从上层剥离）
3. 抓取质量（抓取点置信度、是否可及）
4. 任务匹配（类别、等级、成熟度与任务指令的匹配）
5. 运动状态（速度、是否稳定跟随、是否碰撞后跳变）

多目标时还要给出抓取顺序（按高度从顶部往下），避免抓下层时碰倒上层。

注意：可见比例、遮挡比例是**慢循环的决策依据**，不传给快循环。慢循环已经把"不好抓的"过滤掉了。

### 4.4 下发给快循环的 Goal

只包含选中目标的可见信息：

| 字段 | 说明 |
| --- | --- |
| target_id | 追踪 ID（用于关联与评估） |
| category / grade | 类别、等级（用于力限查表与目的地） |
| target_pose_robot | 机器人基座系位姿（7 维） |
| target_mask | 可见部分 Mask（与 RGB 同帧） |
| target_velocity | 线速度（编码器融合结果） |
| target_size | 尺寸（由 Mask + Depth 估算） |
| force_limit | 抓取力上限（查表） |
| destination_bin | 目标料箱 |
| task_phase | 当前阶段标识 |

## 5. 快循环：技能路由扩散策略

### 5.1 输入

- 视觉：**RGB(3) + Depth(1) + Mask(1) = 5 通道**，同一帧、严格对齐。
- 条件向量：显式位姿（7 维）、线速度（3 维）、尺寸、力限、task_phase，通过 **FiLM** 注入 UNet/DiT 中间层。
- 触觉状态：接触力、滑移标志。

显式位姿必须保留。虽然 RGB-D + Mask 理论上包含位姿信息，但让网络自己从像素学 3D 位置需要数千 episode，且动态场景泛化差；显式位姿把它变成"已知坐标 → 怎么运动"，数据需求降到数百 episode。

### 5.2 专家分类（已最终确定）

5 个专家，**抓取只有一个专家**：

| 专家 | 输入条件 | 输出 | 数据占比 |
| --- | --- | --- | --- |
| Approach Expert | 目标位姿 + 速度 | 关节轨迹 | 30% |
| Grasp Expert | 位姿 + 速度 + 类别 + 尺寸 + 力限 | 关节轨迹 + 夹爪动作 | 25% |
| Lift Expert | 当前位姿 + 目标高度 | 垂直轨迹 | 10% |
| Place Expert | 料箱位姿 + 当前位姿 | 轨迹 + 释放 | 25% |
| Recovery Expert | 失败类型 + 当前状态 | 恢复指令 | 10% |

相比原图的调整：

- 新增 **Lift Expert**：抓取是接触操作、抬起是从力控切回位置控制的过渡，混在一起模型很难学到切换。
- **抓取家族不细分**：Gentle Grasp 与 Normal Grasp 的差别只有一个力控参数，改为 `force_limit` 查表（草莓/番茄 ~5N，苹果/橙子 ~15N）；Dynamic Grasp 与 Normal 的差别只有提前量，而速度已经在输入条件里，模型自己会学。细分只会把本来只占 episode 10-15% 的抓取数据再切三份，每个专家都训不好。
- **Recovery 是"专家 + 规则"**：它由路由器触发，但实际恢复动作大部分由规则表执行，不需要单独训练一个扩散策略。

### 5.3 网络结构

共享视觉编码器 + 共享扩散解码器，专家是轻量适配器（LoRA 或小型 MLP，残差形式，gate 初始化为 0）。这样参数高效、知识共享（轻柔抓取和常规抓取共享物理规律），且路由器可以平滑切换。

Skill Router 输出 5 类权重：

```
approach / grasp / lift / place / recovery
```

### 5.4 训练方案

技能标签**自动生成**，不需要人工标注，用机器人状态判断：

| 阶段 | 自动判据 |
| --- | --- |
| approach | 夹爪张开，末端距目标 > 15cm |
| grasp | 夹爪闭合中 |
| lift | 物体已附着（高度上升、夹爪闭合稳定） |
| place | 到达料箱上方、释放 |
| recovery | 触觉滑移 / 抓取超时 / 力超限 |

两阶段训练：

1. **前 2 个 epoch 硬路由**：用标签强制选择专家，保证每个专家都学到东西，路由器只学分类。
2. **后 2 个 epoch 软路由**：路由器输出权重加权融合专家特征，损失 = 扩散去噪 MSE + 路由分类损失 + 负载均衡损失（防止路由器只用少数专家）。

总 epoch 数 3-4，严重过拟合即死（讨论中引用了 60 demo 任务上的对照：22 epoch 得分 0/60，4 epoch 得分 5/60）。

### 5.5 推理与实时性

- 训练用 DDPM 100 步，推理只改 config 切 **DDIM**（同权重，无需重训），延迟从 393ms 降到 147ms/chunk。
- 部署用 TensorRT，路由器 + 专家 + 解码器一体化推理。
- 用 **Action Chunk + 异步推理线程**摊薄延迟：执行 chunk 期间后台推理下一个 chunk，保证控制周期 20Hz 不断流。
- 实时性要求高时把快循环用 C++ 重写，配合 PREEMPT_RT 内核与 Cyclone DDS。

### 5.6 快循环状态机与异常处理

正常流程：`approach → grasp → lift → place`，由状态机切换，不由模型自己决定。

| 失败类型 | 检测方式 | 恢复动作 |
| --- | --- | --- |
| 滑移 | 触觉 slip 信号 | 加大力 1.5×，重抓 |
| 抓空 | 抓取超时（3s） | 重新接近，重抓 |
| 碰撞 | 力超限 / 电流突变 | 后退 5cm，重规划 |
| 目标丢失 | Mask 消失 > 0.5s | 暂停并请求慢循环重新选目标 |

目标丢失协议：快循环不猜被遮挡部分，进入 paused/lost 状态，向慢循环发 `replanning_requested`，慢循环重新评估后下发新 Goal 或终止。

底层安全护栏：力限、碰撞检测、力超限断电写死在底层（最好用硬件 PLC），不依赖上层 Python。

## 6. 数据采集与训练管线

### 6.1 果蔬类别（8 类，单模型覆盖）

| 类别 | 直径 | 重量 | 抓取特点 |
| --- | --- | --- | --- |
| 草莓 | 2-4cm | 10-30g | 易损，力控要轻 |
| 荔枝 | 2-3cm | 15-25g | 小，易滚 |
| 猕猴桃 | 5-7cm | 80-120g | 绒毛，摩擦大 |
| 番茄 | 5-7cm | 100-200g | 易损 |
| 苹果 | 7-9cm | 150-250g | 光滑，标准 |
| 橙子 | 7-8cm | 150-200g | 粗糙，好抓 |
| 桃子 | 6-8cm | 100-200g | 绒毛，中等 |
| 梨 | 6-8cm | 150-250g | 光滑，标准 |

尺寸跨度 2-9cm、重量 10-250g，单一扩散策略 + 域随机化即可覆盖。**不做**大果组（榴莲、哈密瓜）分组与路由；保留扩展说明：以后要加，只需单独训练 `large_policy` 并在慢循环加一行路由，架构不变。

数据量：每类 50-80 episode，共 400-600 episode，小果多采一些。

### 6.2 仿真场景：动态 + 堆叠

传送带：Isaac Sim Conveyor Belt Utility，速度 0.1-0.5 m/s 随机。

物体生成：不要在初始化时一次性摆放，而是在传送带起点持续 spawn（休眠 → 置位 → 唤醒），随机 yaw 角，允许自然碰撞。以 ≥25Hz 记录每个物体的 6D 位姿 + 线速度 + 角速度 + 可视比例。

堆叠/杂乱：不用手动摆放，把 10-20 个果蔬从料箱上方 0.3-0.5m 随机投放，让物理引擎自然堆积。按难度分级采集：

| 难度 | 物体数 | 堆叠层数 | 用途 |
| --- | --- | --- | --- |
| 简单 | 3-5 | 1 层 | 基础抓取技能 |
| 中等 | 8-12 | 2 层 | 主要训练数据 |
| 困难 | 15-25 | 3+ 层 | 鲁棒性强化 |

### 6.3 域随机化（每个 episode 一次，不是每个 step）

| 维度 | 范围 |
| --- | --- |
| 尺寸 | ±25% |
| 质量 | ±40%（草莓尤其需要） |
| 摩擦系数 | 0.5-1.5 |
| 传送带速度 | 0.1-0.5 m/s |
| 初始位置 / 朝向 | 起点横向偏移 + 随机 yaw |
| 纹理 / 光照 | 颜色抖动、纹理替换 |

原则：从 ±15% 起步逐步加到 ±30%，幅度太大训练不收敛，每个 step 都随机化模型学不到连贯运动。

### 6.4 可见 Mask（不做 amodal）

慢循环只把"基本可见、好抓"的目标交给快循环，所以快循环只需要**实际可见像素**的 Mask（实例分割直接渲染得到），完全在仿真和真机都能获取。

明确不做：amodal mask、完整形状补全、把遮挡比例作为快循环输入。训练时对 Mask 做小幅膨胀/腐蚀与遮挡增强（目标被邻果挡住 20% 左右属正常），让模型对少量遮挡鲁棒，但不处理完全遮挡。

### 6.5 数据格式（LeRobotDataset 扩展）

在标准 LeRobot 格式上扩展：

```
observation.images.rgb        H×W×3
observation.images.depth      H×W×1
observation.images.mask       H×W×1      # 目标的可见 Mask
observation.state.joint      7          # 关节角 + 夹爪
observation.state.ee_pose    7
observation.tactile          6
goal.target_pose_robot       7
goal.target_velocity         3
goal.target_size             1
goal.force_limit             1
goal.destination_bin         N
action.joint                 7
action.gripper               1
meta.skill_label / target_id / execution_status
```

时序：`obs_horizon` 取 2-5 帧（推荐 5），`action_horizon` 取 8-16 步，每条 episode 是"从生成到抓取"的完整时间序列。

可用 DOM（Dynamic Object Manipulation，20 万条动态操作 episode）预训练权重初始化，减少数据需求。

### 6.6 训练配置

在 LeRobot 上改两处：视觉编码器输入通道 3 → 5（新增通道权重用 RGB 权重均值初始化，不要随机），加 GoalEncoder 走 FiLM 注入。3-4 epoch、单张 4090 约 4-6 小时。

辅助损失（可选）：目标中心点 UV 回归，帮助视觉编码器聚焦目标；类别 embedding、物理属性估计网络、元学习全部不做。

## 7. 通信与部署

| 接口 | 方向 | 数据 | 频率 |
| --- | --- | --- | --- |
| `/manipulation_goal` | 慢 → 快 | Goal JSON | 事件驱动 |
| `/tactile_feedback` | 快 → 慢 | 触觉 + 执行状态 | 20Hz / 事件 |
| `/target/velocity` | 编码器 → 全局 | 融合速度 | 30-50Hz |
| `/replanning_requested` | 快 → 慢 | 失败类型 | 事件驱动 |
| 模型文件 | 训练 → 部署 | `.engine` (TensorRT) | 离线 |

中间件 ROS2 Humble + Cyclone DDS；高频状态用共享内存或 Redis；所有状态序列化外置，Agent 崩溃后可恢复。慢循环运行在工控机/服务器，快循环运行在 Jetson AGX Orin 或工控机 + RTX 显卡。

## 8. 建议的实施顺序

1. **先跑通确定性 Workflow**：固定流程（检测 → 规划 → 固定抓取策略）先跑通，再引入 LLM 动态生成 Goal。不要一上来就全自主。
2. **慢循环骨架**：LangGraph + Outlines 结构化输出 + interrupt 人工审批，不依赖硬件，最容易验证。
3. **仿真数据管线**：Isaac Sim 传送带 + 堆叠 + 域随机化 + Mask/位姿导出 + 转 LeRobot 格式。
4. **扩散策略训练**：先单专家（Approach + Grasp + Place）跑通，再加 Lift，最后加 Recovery。
5. **快循环实时化**：先 Python/asyncio 验证时序，再做 TensorRT + C++ + PREEMPT_RT。
6. **Sim-to-Real**：50-100 条真实 episode 微调最后几层 + 真实触觉数据校准力控。

## 9. 主要风险

| 风险 | 应对 |
| --- | --- |
| 手眼标定误差 | 15+ 位姿标定，验证 <5mm，定期复检 |
| 深度反光缺失导致位姿失败 | 多帧融合、结构光相机、Mask 腐蚀 |
| 位姿抖动放大到速度估计 | 低通 + 最小二乘 + 卡尔曼 |
| 推理延迟不满足 20Hz | DDIM + TensorRT + Action Chunk 异步推理 |
| 域随机化幅度不当 | 从 ±15% 起步逐步加，观察收敛 |
| 训练过拟合 | 3-4 epoch 早停，看验证集 |
| 仿真数据直接部署失效 | 真实数据微调 + 在线自适应 |
| 快循环被大模型阻塞 | 大模型绝不进入 5-20Hz 循环，必须解耦 |

## 10. 与原始架构图的差异清单

保留不变的：多模态感知层、结构化环境状态、Experience-Augmented Agent、经验记忆、Skill-Routed Diffusion Policy、共享 Diffusion Decoder、Action Chunk、快慢双循环、触觉反馈闭环。

讨论中做出的调整：

| 原设计 | 最终方案 |
| --- | --- |
| Grasp Family 细分 Normal / Gentle / Dynamic 三个专家 | 合并为 1 个 Grasp Expert；轻柔靠 `force_limit` 查表，动态靠速度条件 |
| 无 Lift 阶段 | 新增 Lift Expert，补上抓取到放置的过渡 |
| Recovery 作为并列专家 | Recovery = 专家模块 + 规则表，由路由器触发 |
| 独立的多专家模型 | 统一网络内的专家适配器 + 共享扩散解码器（MoE 式） |
| 目标条件用 ID / 完整形状 | 位姿 + 可见 Mask；不做 ID embedding，不做 amodal mask |
| 需要从视觉估计速度/角速度 | 传送带编码器为主速度源，位姿差分交叉验证，角速度≈0 |
| 类别 embedding + 物理属性估计 | 全部砍掉，只保留多类别训练 + 域随机化 |
| 大果组（榴莲、哈密瓜）单独策略 | 暂时不做，架构保留扩展位 |
| 元学习、RLHF/DPO 在线微调 | 不做 |
| 光流法速度估计 | 不做主方案，仅作补充 |

## 11. 一句话总结

慢循环用 LangGraph + 经验记忆 + 结构化输出做"选哪个好抓"，快循环用 5 专家技能路由扩散策略（共享解码器）做"怎么抓"，感知用 YOLOv8-seg + ByteTrack + Depth/Mask 反投影 + 传送带编码器，训练在 Isaac Sim 中用 8 类果蔬、400-600 episode、域随机化完成，最后 DDIM + TensorRT 部署到 20Hz 快循环。
